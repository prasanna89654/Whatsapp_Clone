import 'package:flutter/material.dart';

class webScreenLayout extends StatelessWidget {
  const webScreenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is Web format")),
    );
  }
}
